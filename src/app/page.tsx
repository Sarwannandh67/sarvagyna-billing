
import { InvoiceForm } from '@/components/invoice-form';

export default function CreateInvoicePage() {
  return (
    // The main tag is removed as padding and layout are handled by SidebarInset and its main child
    <InvoiceForm />
  );
}
